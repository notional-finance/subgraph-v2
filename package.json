{
  "name": "@notional-finance/subgraph-v2",
  "version": "0.0.1",
  "license": "MIT",
  "scripts": {
    "prepare:local": "hbs --stdout --helper ./helpers.js --data ./v2.local.json ./templates/v3.template.yaml > subgraph.yaml",
    "prepare:arbitrum-one": "hbs --stdout --helper ./helpers.js --data ./v3.arbitrum-one.json ./templates/v3.template.yaml > subgraph.yaml",
    "prepare:mainnet": "hbs --stdout --helper ./helpers.js --data ./v2.mainnet.json ./templates/v3.template.yaml > subgraph.yaml",
    "prepare:goerli": "hbs --stdout --helper ./helpers.js --data ./v2.goerli.json ./templates/v3.template.yaml > subgraph.yaml",
    "codegen": "yarn run graph codegen",
    "build": "yarn run graph build",
    "deploy:local": "graph create --node http://127.0.0.1:8020 notional-finance/local-v2 && graph deploy notional-finance/local-v2 ./subgraph.yaml --ipfs http://localhost:5001 --node http://127.0.0.1:8020 --version-label 0.0.1",
    "deploy:mainnet": "yarn run prepare:mainnet && yarn run codegen && graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ notional-finance/mainnet-v2",
    "deploy:mainnet-debug": "yarn run prepare:mainnet && yarn run codegen && graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ notional-finance/mainnet-debug",
    "deploy:studio:arbitrum-one": "yarn run prepare:arbitrum-one && yarn run codegen && yarn run graph deploy --studio notional-finance-v3-arbitrum",
    "deploy:studio:arbitrum-history": "yarn run prepare:arbitrum-one && yarn run codegen && yarn run graph deploy --studio notional-finance-arb-history",
    "deploy:studio:mainnet-v3": "yarn run prepare:mainnet && yarn run codegen && yarn run graph deploy --studio notional-finance-mainnet-v3",
    "deploy:studio:goerli-v3": "yarn run prepare:goerli && yarn run codegen && yarn run graph deploy --studio notional-finance-goerli-v3",
    "deploy:studio:mainnet": "yarn run prepare:mainnet && yarn run codegen && yarn run graph deploy --studio notional-finance",
    "subgraph:up": "./start.sh",
    "subgraph:down": "docker-compose down",
    "compile:subgraph": "yarn run prepare:local && yarn run codegen && yarn run build && yarn run deploy:local"
  },
  "devDependencies": {
    "@goldskycom/cli": "^1.12.0",
    "@graphprotocol/graph-cli": "^0.60.0",
    "@graphprotocol/graph-ts": "^0.31.0",
    "hbs-cli": "^1.4.0",
    "matchstick-as": "^0.5.2"
  }
}
